# Database (required for app and for npm run db:push)
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=your-password
DB_NAME=billing_app
DB_PORT=3306

# Session (required in production)
SESSION_SECRET=your-random-secret-key-change-this

# Optional
NODE_ENV=development
PORT=3000
